# 应用服务 WEB 访问端口
server:
  port: 8888
spring:
  datasource:
    username: root
    password: 259497Wxh.
    url:  jdbc:mysql://60.204.174.250:3306/batch?serverTimezone=GMT%2B8&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      schema-locations: classpath:org/springframework/batch/core/schema-mysql.sql
      mode: never
#      mode: always
